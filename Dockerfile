# Базовый образ контейнера, на котором в дальнейшем строится основной контейнер
FROM python:3.12-slim

# Рабочая директория приложения
WORKDIR /app

#Копируется файл с зависимостями, и они же и устанавливаются
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt

# Копируются все остальные файлы приложения
COPY . .
